@model IEnumerable<gosteritiyatro.Models.Sahne>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kapasite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tiyatro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kapasite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tiyatro.TiyatroId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SahneId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SahneId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SahneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
